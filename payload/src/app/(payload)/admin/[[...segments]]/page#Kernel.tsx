import { Kernel } from "@payloadcms/next";
import { Options } from "@payloadcms/next/types";

export const Kernel: React.FC<Options> = (props) => {
  return <Kernel {...props} />;
};

export default Kernel;
