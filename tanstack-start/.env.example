# Required - JWT signing secret (minimum 32 characters)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Database (optional - defaults to ./data/mini.db)
# DB_PATH=./data/mini.db

# Environment (production for Docker)
NODE_ENV=production
